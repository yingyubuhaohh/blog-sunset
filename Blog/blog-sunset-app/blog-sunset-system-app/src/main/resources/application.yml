# system
server:
  port: 8090

# 数据库连接
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 123456
    username: root
    url: jdbc:mysql://localhost:3306?blog_sunset&useSSL=true&useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC

# swagger开关
swagger:
  # 后台管理模块api
  system:
    enable: true
  # 前台门户模块api
  portal:
    enable: true